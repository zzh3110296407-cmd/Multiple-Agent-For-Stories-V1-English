# Story Analyzer Frontend

The current analyzer UI is still served by `backend/analyzer_web_ui.py` as an embedded Python HTTP page.

This folder is reserved for the future standalone frontend that will be moved into ProjectCodes.

